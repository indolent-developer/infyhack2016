(function() {

  /*
    This module is responsible for schdule various events.
   */
  'use strict';
  //var jQuery = require('jquery');
  var Jquery = require('jquery'); //eslint-disable-line no-unused-vars

  var scheduler = function (logger) {
    this.logger = logger;
    //this.userRepo = new Repo(config, logger);
  };

  scheduler.prototype = (function () {

    var method1 = function (username, password, callback) {
      // int
    };

    return {
      constructor: eventApi
    };
  }());

  module.exports = scheduler;
}());
